# ServiceAccount for TestCast Runner
# Usage: kubectl apply -f infra/k8s/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: testcast-runner
  namespace: testcast
  labels:
    app: testcast
